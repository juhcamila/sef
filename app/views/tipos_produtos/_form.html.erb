<%= form_with(model: tipos_produto, local: true) do |form| %>
  <% if tipos_produto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipos_produto.errors.count, "error") %> prohibited this tipos_produto from being saved:</h2>

      <ul>
        <% tipos_produto.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :descricao %>
    <%= form.text_field :descricao, class: "form-control" %>
  </div>

  <div class="form-group">
    <button type="submit" class="btn btn-info">Salvar</button>
  </div>
<% end %>